[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install --include=dev && npm run db:generate && npm run build --workspace=apps/api"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
healthcheckPath = "/api/health"
